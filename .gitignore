# dependencies
/node_modules
# next.js
/.next/
/out/
# misc
.DS_Store
*.pem
.env*.local
next-env.d.ts
.vercel
*.tsbuildinfo

# NOTE: data/source/*.html (the Project Madurai source) and data/generated/*.json
# (the parsed dataset) ARE committed on purpose, so the build reads local files
# and never fetches from Project Madurai. Run `npm run import:data` to (re)generate.
